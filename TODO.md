# TODO

## 下一阶段优先项

- [ ] 引入真实画面分析服务，为封面候选提供亮度、清晰度、构图评分和可解释的推荐理由。
- [ ] 定义 `AiMoment` 不可变模型，包含时间戳、标签、置信度、缩略图和分析版本。
- [ ] 明确播放速度与增强效果的产品语义，再扩展 `MediaEngine.exportLive` 和 Android 原生导出参数。
- [ ] 将 Motion Collage 布局选择纳入集中状态，并实现多视频时间同步、音轨优先级和导出策略。
- [ ] 增加页面级 Widget 测试，覆盖小屏布局、模式切换、More Settings、生成状态和无障碍语义。
- [ ] 为缩略图增加内存/磁盘缓存，避免多素材切换与 Widget 重建时重复解码。
- [ ] 增加“减少动态效果”适配，让系统无障碍动效偏好能够关闭呼吸与位移动画。
- [ ] 使用 Room/SQLite 持久化作品索引，让“我的余帧”在 App 重启后从 MediaStore 恢复。
- [ ] 增加相册发布的 Android instrumentation 测试，覆盖 Android 9、10、13 和 14 权限/媒体模型。
- [ ] 增加语言资源键完整性自动校验，防止新增文案只提交单一语言。

## 发布前验证

- [ ] 在常见 Android 屏幕尺寸和横竖视频素材上完成视觉回归。
- [ ] 验证长视频、短于 500ms 的素材、抽帧失败和导出失败场景。
- [ ] 使用真实设备确认玻璃模糊与阴影的 GPU 开销、滚动帧率和内存占用。
- [ ] 对声音、循环、封面、起止范围执行原生导出回归测试。

## 当前明确边界

- 播放速度和增强效果目前只建立 UI 与状态接口，尚未改变原生导出结果。
- 封面推荐目前是本地确定性候选策略，不等同于已上线的 AI 画面理解模型。
- `.live` 容器协议与 Media3 裁剪编码算法未改变；仅将导出结果发布到公共媒体库。
