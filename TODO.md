# TODO

## 下一阶段优先项

- [x] 修复作品删除完成后 Kotlin `Unit` 经 MethodChannel 返回导致的主线程崩溃，并为中断删除、失效 URI、索引残留增加幂等自愈。
- [x] 为“选择视频”媒体库增加真实素材播放预览，并将预览操作与素材选择分离。
- [x] 为“我的余帧”增加 Live 预览、分享、二次确认删除及原生文件/索引清理。
- [x] 修复缩略图内存路径缓存与磁盘淘汰上限不一致导致的滚动后黑屏，并增加原子写入、存活检查和手动重试。

- [ ] 引入真实画面分析服务，为封面候选提供亮度、清晰度、构图评分和可解释的推荐理由。
- [ ] 定义 `AiMoment` 不可变模型，包含时间戳、标签、置信度、缩略图和分析版本。
- [x] 明确播放速度与增强效果的产品语义，再扩展 `MediaEngine.exportLive` 和 Android 原生导出参数。
- [x] 将 Motion Collage 布局选择纳入集中状态，并实现多视频时间同步、音轨优先级和导出策略。
- [x] 增加页面级 Widget 测试，覆盖小屏布局、模式切换、More Settings、生成状态和无障碍语义。
- [x] 为缩略图增加内存/磁盘缓存，避免多素材切换与 Widget 重建时重复解码。
- [x] 增加“减少动态效果”适配，让系统无障碍动效偏好能够关闭呼吸与位移动画。
- [x] 使用 SQLite 持久化作品索引，让“我的余帧”在 App 重启后从 MediaStore 恢复。
- [x] 增加相册发布的 Android instrumentation 契约测试，覆盖 Android 9、10、13 和 14 权限/媒体模型分支。
- [x] 增加语言资源键及动态占位符完整性自动校验，防止新增文案只提交单一语言。

## 发布前验证

- [ ] 在真机上覆盖删除时强杀进程、相册外部删除、私有文件提前丢失和快速重复点击，确认作品索引最终一致。
- [ ] 在 100+ 视频素材库中连续快速滚动、前后台切换和刷新，确认 Pixel、Samsung、小米设备不再出现缩略图黑屏。
- [ ] 在 Android 9/10/13/14/16 真机验证作品预览、删除后系统相册同步消失，以及 App 重启后索引不回生。
- [ ] 在常见 Android 屏幕尺寸和横竖视频素材上完成视觉回归。
- [ ] 验证长视频、短于 500ms 的素材、抽帧失败和导出失败场景。
- [ ] 使用真实设备确认玻璃模糊与阴影的 GPU 开销、滚动帧率和内存占用。
- [ ] 对声音、循环、封面、起止范围执行原生导出回归测试。

## 暂缓项

- 按本阶段范围，真实画面评分服务、`AiMoment` 及其他 AI 能力暂不实现。
- 封面候选继续使用本地确定性时间位置策略，不产生亮度、清晰度或构图分数。
- Android 9/10/13/14 的权限分支已有 instrumentation 契约测试，仍需在对应真实设备或托管设备矩阵执行发布回归。
