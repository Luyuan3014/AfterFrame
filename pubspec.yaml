name: after_frame
description: "AfterFrame · 余帧 — 将视频里的珍贵瞬间转化为动态记忆。"
publish_to: 'none'
version: 0.4.0+4

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  video_player: ^2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
